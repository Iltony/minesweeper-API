
import { FC } from "react"
import { RegisterProps } from "../Utils/Interfaces";

const RegisterComponent:FC<RegisterProps> = (props: RegisterProps) =>
{
   return (
      <h4 className="welcome">Register</h4>
   );
}

export default RegisterComponent;